{
  "name": "senior-project",
  "private": true,
  "workspaces": [
    "frontend/",
    "backend/"
  ],
  "scripts": {
    "offline": "nodemon --watch backend --watch serverless.yml --exec serverless offline"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "serverless-offline": "^14.4.0",
    "serverless-s3-sync": "^3.4.0"
  }
}
